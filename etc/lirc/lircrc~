begin
button = power
prog = irexec
config = vcgencmd display_power 0
end


begin
button = play
prog = irexec
config = sh /home/radipi/lircScript/startBrowser.sh
end

#begin
#button = recordlist
#prog = irexec
#config = redcnt=`awk -F "=" '{print $2}' /tmp/cnt.txt`; md=`cat /home/radipi/radio/mntList.txt | wc -l`; mdd=$(((redcnt % md)+1));echo $mdd; mntPath=`sed -n ${mdd}p /home/radipi/radio/mntList.txt`; /home/radipi/script/localScript/killsound.sh KILL ; /home/radipi/script/localScript/setPCMlocal.sh 86; /home/radipi/script/playmp3.sh ${mntPath} /tmp/vpm.socket 10
#end
#
#begin
#button = reservecheck
#prog = irexec
#config = redcnt=`awk -F "=" '{print $2}' /tmp/cnt.txt`; md=`cat /home/radipi/radio/recRadio.txt | wc -l`; mdd=$(((redcnt % md)+1));echo $mdd; mntPath=`sed -n ${mdd}p /home/radipi/radio/recRadio.txt`; /home/radipi/script/localScript/killsound.sh KILL ; /home/radipi/script/localScript/setPCMlocal.sh 92; /home/radipi/script/playmp3.sh ${mntPath} /tmp/vpm.socket 3 
#end


begin
button = up
prog = irexec
config = sh /home/radipi/lircScript/onekey.sh "Up"
end

begin
button = down
prog = irexec
config = sh /home/radipi/lircScript/onekey.sh "Down"
end

begin
button = left
prog = irexec
config = sh /home/radipi/lircScript/onekey.sh "ctrl+minus"
end


begin
button = right
prog = irexec
config = sh /home/radipi/lircScript/onekey.sh "ctrl+plus"
end
#
#
#begin
#button = backward
#prog = irexec
#config = echo '{ "command": ["seek", -600000] }' | socat - /tmp/vpm.socket
#end
#
#begin
#button = forward
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh INT
#end

begin
button = stop
prog = irexec
config = sh /home/radipi/lircScript/quitBrowser.sh
end

begin
button = dubbing
prog = irexec
config = sh /home/radipi/lircScript/refresh.sh
end

#begin
#button = decide
#prog = irexec
#config = redcnt=`grep -o "[0-9]*" /tmp/cnt.txt ` ;redcnt=$(( redcnt + 1 )) ; echo "counter="$redcnt > /tmp/cnt.txt
#end









#begin
#button = inputSW
#prog = irexec
#config = echo 'cycle pause' | socat - /tmp/vpm.socket
#end
#
#begin
#button = broadSW
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL
#end
#
#begin
#button = volup
#prog = irexec
#config = /home/radipi/script/localScript/setPCMVolumeLocal.sh + 3
#end
#
#begin
#button = voldown
#prog = irexec
#config = /home/radipi/script/localScript/setPCMVolumeLocal.sh - 3
#end
#
#
#begin
#button = d
#prog = irexec
#config = echo '{ "command": ["seek", -600000] }' | socat - /tmp/vpm.socket
#end
#
#begin
#button = back
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh INT
#end
#
#begin
#button = menu
#prog = irexec
#config = redcnt=`awk -F "=" '{print $2}' /tmp/cnt.txt`; md=`cat /home/radipi/radio/mntList.txt | wc -l`; mdd=$(((redcnt % md)+1));echo $mdd; mntPath=`sed -n ${mdd}p /home/radipi/radio/mntList.txt`; /home/radipi/script/localScript/killsound.sh KILL ; /home/radipi/script/localScript/setPCMlocal.sh 86; /home/radipi/script/playmp3.sh ${mntPath} /tmp/vpm.socket 10
#end
#
#begin
#button = program
#prog = irexec
#config = redcnt=`awk -F "=" '{print $2}' /tmp/cnt.txt`; md=`cat /home/radipi/radio/recRadio.txt | wc -l`; mdd=$(((redcnt % md)+1));echo $mdd; mntPath=`sed -n ${mdd}p /home/radipi/radio/recRadio.txt`; /home/radipi/script/localScript/killsound.sh KILL ; /home/radipi/script/localScript/setPCMlocal.sh 92; /home/radipi/script/playmp3.sh ${mntPath} /tmp/vpm.socket 3 
#end
#
#begin
#button = blue
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd3/radio/tokyopod /tmp/vpm.socket 1 
#end
#
#
#begin
#button = red
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd3/radio/ライムスター宇多丸_ウィークエンド・シャッフル /tmp/vpm.socket 1 
#end
#
#begin
#button = green
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd3/radio/pinsoba /tmp/vpm.socket 1 
#end
#
#begin
#button = yellow
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd3/rakugo /tmp/vpm.socket 1 
#end
#
#
#begin
#button = 1
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd1/\[FreeStyleDungeon\_base\]/RHYMESTER /tmp/vpm.socket 5 
#end
#
#begin
#button = 2
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd1/artist/たま /tmp/vpm.socket 5 
#end
#
#begin
#button = 3
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd1/artist/電気グルーヴ /tmp/vpm.socket 5 
#end
#
#begin
#button = 4
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd2/rakugo/桂米朝 /tmp/vpm.socket 2 
#end
#
#begin
#button = 5
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd2/rakugo/桂枝雀 /tmp/vpm.socket 2 
#end
#
#begin
#button = 6
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playVideo.sh /mnt/tpd3/video/papepo/ /tmp/vpm.socket 3
#end
#
#
#
#begin
#button = 7 
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd3/radio/くりぃむしちゅーのオールナイトニッポン /tmp/vpm.socket 2 
#end
#
#begin
#button = 8
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd3/random_radio /tmp/vpm.socket 2
#end
#
#begin
#button = 9
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd2/rakugo/古今亭志ん朝 /tmp/vpm.socket 2 
#end
#
#
#begin
#button = 10
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/playmp3.sh /mnt/tpd2/rakugo/柳家小さん /tmp/vpm.socket 2 
#end
#
#begin
#button = 11
#config = /home/radipi/script/localScript/killsound.sh KILL; /home/radipi/script/localScript/playByTime.sh /home/radipi/script/config/timeTable;
#prog = irexec
#end
#
#
#begin
#button = 12
#prog = irexec
#config = /home/radipi/script/localScript/killsound.sh KILL; redcnt=`awk -F "=" '{print $2}' /tmp/cnt.txt`; mdd=$(((redcnt % 2)+1)); echo $mdd; if [ $mdd -eq 2 ]; then cat /var/www/html/config/streamingList.csv | grep GOLD | cut -d "," -f 2 | xargs mpv --msg-level=all=info; else /home/radipi/script/localScript/playradikoLocal.sh -p TBS; fi
#end
#
#
#
#begin
#button = powerelpa
#prog = irexec
#config = sudo reboot
#end
#
#
#
#begin
#button = decide
#prog = irexec
#ignore_first_events=10
#config = sudo service networking restart
#end
